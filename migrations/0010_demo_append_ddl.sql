-- DEMO: SECOND structural push while the first ticket is still open -> dedup appends this script
-- as a comment on the existing ticket instead of creating a new one.
DROP TABLE IF EXISTS omnisync.dsarc_demo_noop_2;
